## Summary
<!-- Brief description of what this PR does and why these changes were made. -->

## Ticket
Closes STX-

## Type of Change
- [ ] New Feature (`feature/`)
- [ ] Bug Fix (`bugfix/`)
- [ ] Improvement (`improvement/`)
- [ ] Hotfix (`hotfix/`)

## Affected Monorepo Packages
- [ ] `backend` (API - port 4000)
- [ ] `frontend` (Next.js Public Site - port 3000)
- [ ] `admin` (Next.js Admin Console - port 3001)

## Changes Made
<!-- List detailed changes made in this PR -->
- 

## Screenshots / Evidence
<!-- 
PRs without screenshots or testing evidence will be rejected immediately.
- UI changes: Before & after screenshots.
- API changes: Request/response captures (Postman, Thunder Client, etc.).
- Bug fixes: Screenshot/log proving the issue is resolved.
- Console/logic: Output logs demonstrating correct behavior.
-->


## Testing Done Checklist
- [ ] The application runs without errors locally (`npm run dev`).
- [ ] Code is linted successfully (`npm run lint`).
- [ ] Build compiles successfully (`npm run build`).
- [ ] Specific feature / bug fix / improvement works as expected.
- [ ] No existing functionality has been broken.

## Changelog Updated
- [ ] Yes — entry added under `[Unreleased]` in `CHANGELOG.md` with the ticket ID in parentheses.
  - Category used: `Added` | `Fixed` | `Changed` | `Removed` | `Security`

## Git Workflow Check
- [ ] My branch was created from `development` (not `main`).
- [ ] My branch name follows the rule: `<type>/<TICKET-ID>` (e.g., `feature/STX-FEAT-0120`).
- [ ] My branch has **exactly one commit** (squashed if necessary).
- [ ] My commit message follows the format: `<type>(ticket): <clear description>` (e.g., `feat(STX-FEAT-0120): implement user profile avatar upload`).
